module.exports = {
  reactStrictMode: true, // Optional but helps with debugging React issues
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'utfs.io',
      },
    ],
  },
};